---
post:
  summary: "Create Fixtures filtered"
  requestBody:
    required: true
    content:
      application/json:
        schema:
          type: object

  responses:
    "200":
      description: "Success"
      content:
        application/json:
          schema:
            type: object
    "400":
      description: "Bad Request"
      content:
        application/json:
          schema:
            $ref: "../../components/schemas/Error.yaml"
    "500":
      description: "Internal Server Error"
      content:
        application/json:
          schema:
            $ref: "../../components/schemas/Error.yaml"
