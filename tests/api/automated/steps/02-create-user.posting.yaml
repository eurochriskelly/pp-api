name: 02.create-user
phase: setup
method: POST
path: /api/users
headers:
  Content-Type: application/json
body:
  email: $TEST_EMAIL
  name: $TEST_NAME
  password: $TEST_PASSWORD
  role: organizer
expect:
  status: 201
  json:
    - path: id
      exists: true
capture:
  USER_ID: id
